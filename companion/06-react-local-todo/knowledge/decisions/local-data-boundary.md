# Local data boundary

The starter reads and writes only its own fictional task array under one browser
`localStorage` key. It sends no task data to a service. Browser storage is not a
backup or a security guarantee; clearing it can remove the learning data.

# Task model

A task has a stable `id`, a non-empty `title`, and a boolean `completed` value.
This starter supports creating, toggling, and deleting a single task. It has no
accounts, sharing, dates, tags, notifications, or remote synchronization.

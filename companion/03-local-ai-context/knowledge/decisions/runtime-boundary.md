# Runtime boundary

Start with a local, read-only discovery task. Record `ollama ps` observations
while the model is active. Local execution does not eliminate the need for
source review, narrow paths, synthetic data, or human verification.

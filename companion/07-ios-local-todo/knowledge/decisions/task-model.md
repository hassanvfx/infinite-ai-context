# Task model

A task has an id, a non-empty title, and a completed Boolean. The lab supports
create, toggle, and delete only. Dates, tags, editing, accounts, sharing, and
synchronization require separate future decisions.
